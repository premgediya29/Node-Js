<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Movie Data</title>
    <style>
        :root {
            --primary-color: #315cb3;
            --hover-color: #315cb3;
            --background-color: #f4f4f9;
            --form-bg-color: #fff;
            --text-color: #333;
            --placeholder-color: #999;
            --box-shadow-light: rgba(0, 0, 0, 0.1);
            --box-shadow-heavy: rgba(0, 0, 0, 0.2);
            --container-width: 350px;
        }

        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--background-color);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        h1 {
            color: var(--text-color);
            font-size: 2.5em;
            margin-bottom: 20px;
            text-align: center;
        }

        .container {
            background-color: var(--form-bg-color);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px var(--box-shadow-light);
            width: var(--container-width);
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: transform 0.2s ease;
        }

        .container:hover {
            transform: translateY(-5px);
        }

        form {
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 15px;
            align-items: center;
        }

        input[type="file"],
        input[type="text"],
        input[type="number"],
        input[type="submit"] {
            padding: 10px;
            font-size: 1em;
            border-radius: 5px;
            border: 1px solid #ccc;
            width: 100%;
            box-sizing: border-box;
        }

        input[type="submit"] {
            background-color: var(--primary-color);
            color: white;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: var(--hover-color);
        }

        input::placeholder {
            color: var(--placeholder-color);
            font-size: 0.9em;
        }

        img {
            margin-bottom: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px var(--box-shadow-heavy);
            width: 30%;
            height: auto;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        img:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 12px var(--box-shadow-heavy);
        }

        @media (max-width: 768px) {
            .container {
                width: 90%;
            }

            h1 {
                font-size: 2em;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Update Data</h1>
        <form action="/updatemovie?id=<%=editschema.id%>" method="post" enctype="multipart/form-data">
            <img src="<%=editschema.image%>" alt="Movie Poster">
            <input type="text" name="name" placeholder="Edit movie name" value="<%=editschema.name%>">
            <input type="text" name="movietype" placeholder="Edit movie type" value="<%=editschema.movietype%>">
            <input type="number" name="rating" placeholder="Edit rating" value="<%=editschema.rating%>">
            <input type="file" name="image">
            <input type="submit" value="Update">
        </form>
    </div>
</body>

</html>
